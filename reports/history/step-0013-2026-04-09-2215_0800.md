# Step 13: Set up timeline content collection with Gloria Santona event

**Commit:** `[step-13] Set up timeline content collection with Gloria Santona event` — 2026-04-09 22:15:20 +0800

## What was done
- Created `src/content.config.ts` with `timeline` collection schema (Zod):
  - Uses `glob` loader: `pattern: "**/*.mdx", base: "./src/content/timeline"`
  - Required fields: title, description, date, slug
  - Optional: lastmod, draft, tags, timelines, featuredImage, CONF_websites, etc.
- Converted Hugo `index.en.md` to `src/content/timeline/2023-01-30-gloria-santona/index.mdx`:
  - Replaced all Hugo shortcodes with MDX component syntax
  - `CONF_websites` stored as space-separated string (original Hugo format was ambiguous multiline)
  - Converted `<br>` to self-closing `<br/>`
- Copied 5 PNGs + 5 PDFs into the content folder
- Updated `src/middleware.ts` — added `SITE_DOMAIN` env var (default: `ecthrwatch.org`)
- Updated `src/env.d.ts` — added `siteDomain: string` to `App.Locals`

## Issues
- Hugo's `CONF_websites` field uses inconsistent YAML formatting (multiline string without proper list syntax). Stored as a space-separated string and use `.includes()` for filtering.
