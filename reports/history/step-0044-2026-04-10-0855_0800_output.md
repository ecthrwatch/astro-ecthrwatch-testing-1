# Output — Add tracking system rules to CLAUDE.md

**Date:** 2026-04-10 ~08:55 +0800
**Commit:** `[step-24]` (pending)

---

## Summary

- `CLAUDE.md` only had safety/commit rules — no tracking system instructions
- Added full "INPUT/OUTPUT TRACKING SYSTEM (MANDATORY)" section covering:
  - File structure and naming convention
  - Sequential interleaved numbering
  - Output file format (Summary + Verbatim Output)
  - 7 rules (never delete, verbatim inputs, write output LAST, copy to latest-report.md, commit, +0800 timestamps, continue numbering across sessions)
  - RESPONSE SEQUENCE — explicit 6-step order to follow every time (fixes the "always 1 step behind" problem)
- Existing safety rules preserved unchanged below the new section

---

## Verbatim Output

Yes — `CLAUDE.md` is exactly that config file. Future Claude Code sessions read it automatically at the start. Right now ours only has the safety/commit rules but nothing about the tracking system. Let me show you what's there:

> **Tool: Read** — Read `CLAUDE.md`
> Result: Only 31 lines — safety rules about committing. No tracking system rules at all.

See — no tracking rules at all. Project_1's CLAUDE.md actually has them (that was the one thing they got right). Let me add the tracking system rules to ours. I'll keep the existing safety rules and add the tracking section:

> **Tool: Write** — Created `step-0043-2026-04-10-0855_0800_input.md` (user's message)

> **Tool: Edit** — Added tracking system section to `CLAUDE.md` before the safety rules. New section includes:
> - Structure: `reports/history/` + `reports/latest-report.md`
> - File naming: `step-NNNN-YYYY-MM-DD-HHMM_0800_input.md` / `_output.md`
> - Numbering: Sequential interleaved (odd=input, even=output)
> - Output format: Summary + Verbatim Output sections
> - 7 rules (never delete, verbatim, write output LAST, copy to latest-report.md, commit, +0800 timestamps, continue numbering)
> - RESPONSE SEQUENCE: explicit 6-step order to follow every time

The CLAUDE.md now has everything a future session needs to continue the tracking system correctly. The key addition is the "RESPONSE SEQUENCE" which explicitly says to write the output file LAST and display nothing after the commit — fixing the "always 1 step behind" problem permanently.
