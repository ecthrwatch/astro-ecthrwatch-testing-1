# Output — Fix site name: all sites showed "ECTHRwatch.org"

**Date:** 2026-04-12 ~11:15 +0800
**Commit:** `[fix] Use site-config.ts for site name instead of hardcoded translations`

---

## Summary

- **Root cause:** `shared/i18n/translations.ts` had `"site.title": "ECTHRwatch.org"` hardcoded. Nav and Base layout both called `t(locale, "site.title")`, so every site showed "ECTHRwatch.org" regardless.
- **Fix:** Site name now flows through `Astro.locals.siteName`, set by each site's middleware from its own `site-config.ts`.
- **Changes:**
  - `shared/components/Nav.astro` — brand link reads `Astro.locals.siteName` instead of `t(locale, "site.title")`
  - `shared/layouts/Base.astro` — `<title>` reads `Astro.locals.siteName`
  - All 6 sites' `src/middleware.ts` — imports `siteConfig`, sets `context.locals.siteName = siteConfig.name`
  - All 6 sites' `src/env.d.ts` — added `siteName: string` to Locals
- **Verified:** ecthrwatch.org builds (20 pages), thechristianintegrity.com builds (1 page) — each now shows its own name

---

## Verbatim Output

Because the shared `translations.ts` has `site.title` hardcoded as "ECTHRwatch.org", and all sites share it. The Nav and Base layout pull the site name from there. The `site-config.ts` files we created have the correct names but nothing reads from them yet.

Let me trace the exact chain and fix it:

> **Tool: Read** — `shared/i18n/translations.ts` line 55: `"site.title": "ECTHRwatch.org"` — hardcoded
> **Tool: Read** — `shared/components/Nav.astro` line 24: `{t(locale, "site.title")}` — reads from translations
> **Tool: Read** — `sites/thechristianintegrity.com/src/site-config.ts` — has `name: "The Christian Integrity"` but nothing uses it

The fix: pass the site name through `Astro.locals` (set by middleware, which IS per-site and already has access to `site-config.ts`).

> **Tool: Edit** — `sites/ecthrwatch.org/src/middleware.ts`:
> - Added `import { siteConfig } from "./site-config";`
> - Set `context.locals.siteName = siteConfig.name` in onRequest
> - Used `siteConfig.domain` as default for siteDomain

> **Tool: Edit** — `sites/ecthrwatch.org/src/env.d.ts`:
> - Added `siteName: string;` to Locals interface

> **Tool: Edit** — `shared/components/Nav.astro`:
> - Added `const siteName = Astro.locals.siteName ?? "Site";`
> - Changed brand link from `{t(locale, "site.title")}` to `{siteName}`

> **Tool: Edit** — `shared/layouts/Base.astro`:
> - Added `const siteName = Astro.locals.siteName ?? "Site";`
> - Changed `<title>` from `{t(locale, "site.title")}` to `{siteName}`

> **Tool: Agent** — Updated all 5 skeleton sites' middleware.ts and env.d.ts with same changes

> **Tool: Bash** — Verified builds:
> - `sites/ecthrwatch.org`: 20 pages ✅
> - `sites/thechristianintegrity.com`: 1 page ✅

> **Tool: Bash** — Committed: `[fix] Use site-config.ts for site name instead of hardcoded translations`
