# Step 14: Create timeline list and event page routes

**Commit:** `[step-14] Create timeline list and event page routes` — 2026-04-09 22:17:20 +0800

## What was done
- Created `src/pages/timeline-of-events-and-open-letters/index.astro`:
  - Fetches events via `getCollection("timeline")`
  - Filters by `!draft` and `CONF_websites.includes(siteDomain)`
  - Sorts reverse chronologically
  - Renders as `<ol>` with red date + description (matching Hugo layout)
- Created `src/pages/timeline-of-events-and-open-letters/[...slug].astro`:
  - `getStaticPaths()` generates routes from event slugs
  - Renders `<h1>` title + `<Content />` MDX
  - Styled blockquotes, horizontal rules, figures
- Updated `src/pages/[slug].astro` — excluded `nav.timeline` from `getStaticPaths()`

## Issues
- None — build succeeds with 15 pages
