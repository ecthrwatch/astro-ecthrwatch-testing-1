# Output — Update import paths to use @shared alias

**Date:** 2026-04-12 ~10:00 +0800

---

## Summary

Updated all import paths in 5 files under `sites/ecthrwatch.org/src/` to use the `@shared/*` path alias instead of relative paths. Also reported contents of `env.d.ts`.

### Files changed

1. **`src/pages/index.astro`** (2 imports updated)
   - `"../layouts/Base.astro"` -> `"@shared/layouts/Base.astro"`
   - `"../i18n/translations"` -> `"@shared/i18n/translations"`

2. **`src/pages/[slug].astro`** (5 imports updated)
   - `"../layouts/Base.astro"` -> `"@shared/layouts/Base.astro"`
   - `"../components/AiAnalysisContent.astro"` -> `"@shared/components/AiAnalysisContent.astro"`
   - `"../i18n/utils"` -> `"@shared/i18n/utils"`
   - `"../i18n/translations"` -> `"@shared/i18n/translations"` (2 lines: type import + named import)

3. **`src/pages/timeline-of-events-and-open-letters/index.astro`** (3 imports updated)
   - `"../../layouts/Base.astro"` -> `"@shared/layouts/Base.astro"`
   - `"../../i18n/utils"` -> `"@shared/i18n/utils"`
   - `"../../i18n/translations"` -> `"@shared/i18n/translations"`

4. **`src/pages/timeline-of-events-and-open-letters/[...slug].astro`** (2 imports updated)
   - `"../../layouts/Base.astro"` -> `"@shared/layouts/Base.astro"`
   - `"../../i18n/translations"` -> `"@shared/i18n/translations"`

5. **`src/content.config.ts`** (1 path updated)
   - `base: "./src/content/timeline"` -> `base: "../../content/timeline/en"`

### env.d.ts contents (reported, not modified)

```typescript
/// <reference types="astro/client" />

declare namespace App {
  interface Locals {
    locale: import("./i18n/translations").Locale;
    siteDomain: string;
  }
}
```

Note: `env.d.ts` references `"./i18n/translations"` which is a relative path from `src/`. This will need updating if the i18n/translations module has been moved to shared/. The correct path would be `@shared/i18n/translations` or a relative path to the shared directory.

---

## Verbatim Output

All 5 files updated successfully. Here is confirmation of each edit:

**1. `src/pages/index.astro`** -- CONFIRMED:
```
import Base from "@shared/layouts/Base.astro";
import type { Locale } from "@shared/i18n/translations";
```

**2. `src/pages/[slug].astro`** -- CONFIRMED:
```
import Base from "@shared/layouts/Base.astro";
import AiAnalysisContent from "@shared/components/AiAnalysisContent.astro";
import { t } from "@shared/i18n/utils";
import type { Locale } from "@shared/i18n/translations";
import { navSlugs, navKeys } from "@shared/i18n/translations";
```

**3. `src/pages/timeline-of-events-and-open-letters/index.astro`** -- CONFIRMED:
```
import Base from "@shared/layouts/Base.astro";
import { getCollection } from "astro:content";
import { t } from "@shared/i18n/utils";
import type { Locale } from "@shared/i18n/translations";
```

**4. `src/pages/timeline-of-events-and-open-letters/[...slug].astro`** -- CONFIRMED:
```
import Base from "@shared/layouts/Base.astro";
import { getCollection, render } from "astro:content";
import type { Locale } from "@shared/i18n/translations";
```

**5. `src/content.config.ts`** -- CONFIRMED:
```
loader: glob({ pattern: "**/*.mdx", base: "../../content/timeline/en" }),
```

**6. `src/env.d.ts`** -- READ (not modified):
```typescript
/// <reference types="astro/client" />

declare namespace App {
  interface Locals {
    locale: import("./i18n/translations").Locale;
    siteDomain: string;
  }
}
```

This file uses `import("./i18n/translations")` which resolves relative to `src/`. If `i18n/translations` no longer exists at `src/i18n/translations`, this will need updating to either `@shared/i18n/translations` or a relative path like `../../shared/i18n/translations`.
