# Output — Multi-site restructuring complete (Steps 1–3)

**Date:** 2026-04-12 ~11:00 +0800
**Commits:**
- `[step-28] Restructure for multi-site: move ecthrwatch.org to sites/, shared code to shared/, content to content/`
- `[step-28] Update import paths to use @shared/* alias after project restructure`
- `[step-28] Scaffold 5 new site directories + ecthrwatch.org site-config.ts`
- `[step-28] Record step-0054 input + step-0055 output (scaffold 5 new sites)`

---

## Summary

Complete multi-site restructuring done in 3 steps:

### STEP 1+2: Restructure + move shared code
- Moved site-specific files (pages, middleware, config) → `sites/ecthrwatch.org/`
- Moved shared code (components, data, i18n, layouts, styles) → `shared/`
- Moved timeline content → `content/timeline/en/`
- Added TypeScript path alias `@shared/*` → `../../shared/*`
- Updated `loader.ts` to resolve data paths from `sites/<domain>/` CWD → `../../shared/data/`
- Updated all import paths (pages use `@shared/...`, MDX uses `../../../../shared/components/...`)
- Build verified: 20 pages in 2.75s

### STEP 3: Create 5 skeleton sites
All building successfully (1 page each):

| Site | Domain Config | Default Locale |
|------|---------------|----------------|
| theamericanintegrity.com | www/fr/zh subdomains | en |
| thechristianintegrity.com | www/fr/zh subdomains | en |
| vaticanwatch.org | www/fr/zh subdomains | en |
| agreaterchina.com | www/fr/zh subdomains | en |
| hepinggongchu.org | Cross-domain (separate .org domains) | zh-cn |

### Final project structure
```
claude_code_project_018_astro/
├── shared/               ← components, data, i18n, layouts, styles
├── content/
│   └── timeline/
│       └── en/           ← 6 migrated events
├── sites/
│   ├── ecthrwatch.org/   ← full site (20 pages)
│   ├── theamericanintegrity.com/  ← skeleton (1 page)
│   ├── thechristianintegrity.com/ ← skeleton (1 page)
│   ├── vaticanwatch.org/          ← skeleton (1 page)
│   ├── agreaterchina.com/         ← skeleton (1 page)
│   └── hepinggongchu.org/        ← skeleton (1 page)
├── reports/
└── CLAUDE.md
```

Each site has its own `src/site-config.ts` with domain-to-language mapping.

---

## Verbatim Output

Let me stop the preview server first, then start the restructuring:

> **Tool: Bash** — Stopped preview server

Commit current state before destructive operations:

> **Tool: Bash** — `git add reports/history/step-0051-... && git commit`
> `[pre-restructure] Save step-0051 input before multi-site restructuring`

### STEP 1: Move ecthrwatch.org into sites/ecthrwatch.org/

> **Tool: Bash** — `mkdir -p sites/ecthrwatch.org/src`

> **Tool: Bash** — Moved site-specific files:
> - `src/pages/` → `sites/ecthrwatch.org/src/pages/`
> - `src/middleware.ts` → `sites/ecthrwatch.org/src/middleware.ts`
> - `src/env.d.ts` → `sites/ecthrwatch.org/src/env.d.ts`
> - `src/content.config.ts` → `sites/ecthrwatch.org/src/content.config.ts`
> - `public/` → `sites/ecthrwatch.org/public/`
> - `astro.config.mjs` → `sites/ecthrwatch.org/astro.config.mjs`
> - `tsconfig.json` → `sites/ecthrwatch.org/tsconfig.json`
> - Copied `package.json` and `package-lock.json`

Now update the import paths in the moved files. The pages import from `../../layouts/Base.astro` etc. — those files are still in `src/` at the project root. Rather than fixing relative paths twice (once for Step 1, again for Step 2), I decided to do Steps 1 and 2 together. The files need to point to their final location.

### STEP 2: Move shared code into shared/

> **Tool: Bash** — `mkdir -p shared content/timeline/en`

> **Tool: Bash** — Moved shared code:
> - `src/components/` → `shared/components/`
> - `src/data/` → `shared/data/`
> - `src/i18n/` → `shared/i18n/`
> - `src/layouts/` → `shared/layouts/`
> - `src/styles/` → `shared/styles/`
> - `src/content/timeline/*` → `content/timeline/en/`
> - Removed empty `src/` directory

> **Tool: Bash** — Verified structure: shared/ has components, data, i18n, images, layouts, styles. content/timeline/en/ has all 6 events. sites/ecthrwatch.org/src/ has content.config.ts, env.d.ts, middleware.ts, pages.

> **Tool: Bash** — Restored `content/migration-tracker.md` from git (was in `src/content/`, not inside `timeline/`)

> **Tool: Write** — Updated `sites/ecthrwatch.org/tsconfig.json` — added path alias:
> ```json
> {
>   "extends": "astro/tsconfigs/strict",
>   "compilerOptions": {
>     "baseUrl": ".",
>     "paths": {
>       "@shared/*": ["../../shared/*"]
>     }
>   }
> }
> ```

> **Tool: Edit** — Updated `sites/ecthrwatch.org/src/middleware.ts`:
> - `"./i18n/translations"` → `"@shared/i18n/translations"`

> **Tool: Agent** — Updated ALL import paths in pages:
> - `index.astro`: Base → `@shared/layouts/Base.astro`, translations → `@shared/i18n/translations`
> - `[slug].astro`: Base, AiAnalysisContent, utils, translations → all `@shared/...`
> - Timeline pages: same pattern
> - `content.config.ts`: `base: "./src/content/timeline"` → `"../../content/timeline/en"`

> **Tool: Edit** — Updated `sites/ecthrwatch.org/src/env.d.ts`:
> - `import("./i18n/translations")` → `import("@shared/i18n/translations")`

Now update the shared files — `loader.ts` needs to resolve paths to `shared/data/`:

> **Tool: Edit** — Updated `shared/data/loader.ts`:
> - `path.join(process.cwd(), "src/data")` → `path.join(process.cwd(), "..", "..", "shared", "data")`
> - (First tried `import.meta.url` but that doesn't survive Astro's build bundling — the URL points to `dist/.prerender/chunks/` instead of source)

> **Tool: Bash** — Checked relative imports in shared code: `../data/loader`, `../i18n/translations`, `../components/Nav.astro` — all still correct because shared/ maintains the same internal structure.

Now update the MDX files — they import components with relative paths that broke when content moved:

> **Tool: Bash** — Found MDX imports: `'../../../components/Dict.astro'` etc.

These paths (`../../../components/`) pointed to `src/components/` when content was in `src/content/timeline/`. Now content is in `content/timeline/en/`, so the path needs to go to `shared/components/`. From `content/timeline/en/<event>/`, the path to `shared/components/` is `../../../../shared/components/`:

> **Tool: Bash** — `sed` replaced all MDX imports:
> `'../../../components/'` → `'../../../../shared/components/'`
> Updated 6 import statements across 3 MDX files.

> **Tool: Write** — Updated `.claude/launch.json` — added `"cwd": "sites/ecthrwatch.org"` to all 3 dev server configs.

> **Tool: Bash** — `cd sites/ecthrwatch.org && npm install` — 0 vulnerabilities

> **Tool: Bash** — `LOCALE=en SITE_DOMAIN=ecthrwatch.org npx astro build`
> First attempt FAILED: `ENOENT: no such file or directory, open '.../dist/.prerender/chunks/dictionaries/en/original.yaml'`
> - Root cause: `import.meta.url` in loader.ts resolves to the bundled location during build, not the source
> - Fix: switched to `path.join(process.cwd(), "..", "..", "shared", "data")`
> Second attempt: ✓ **20 pages built in 2.75s**

> **Tool: Bash** — `git add -A && git commit`
> `[step-28] Restructure for multi-site: move ecthrwatch.org to sites/, shared code to shared/, content to content/`
> 71 files changed

### STEP 3: Create 5 skeleton sites

> **Tool: Agent** — Created all 5 sites with 8 files each:
> - theamericanintegrity.com ✓ (1 page, 1.84s)
> - thechristianintegrity.com ✓ (1 page, 1.82s)
> - vaticanwatch.org ✓ (1 page, 1.86s)
> - agreaterchina.com ✓ (1 page, 1.95s)
> - hepinggongchu.org ✓ (1 page, 1.88s) — zh-cn primary, cross-domain routing

> Also created `sites/ecthrwatch.org/src/site-config.ts` for consistency.

> Agent committed: `[step-28] Scaffold 5 new site directories + ecthrwatch.org site-config.ts`

Multi-site restructuring complete. Final state:

```
claude_code_project_018_astro/
├── shared/               ← components, data, i18n, layouts, styles
├── content/timeline/en/  ← 6 migrated events
├── sites/
│   ├── ecthrwatch.org/           ← 20 pages ✅
│   ├── theamericanintegrity.com/ ← 1 page ✅
│   ├── thechristianintegrity.com/← 1 page ✅
│   ├── vaticanwatch.org/         ← 1 page ✅
│   ├── agreaterchina.com/        ← 1 page ✅
│   └── hepinggongchu.org/        ← 1 page ✅ (zh-cn primary)
├── reports/
└── CLAUDE.md
```

All 6 sites build successfully. Each has its own `site-config.ts` with domain-to-language mapping. To run ecthrwatch.org dev: `cd sites/ecthrwatch.org && npm run dev:en`.
